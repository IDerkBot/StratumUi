<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:attachedProperties="clr-namespace:StratumUi.Wpf.Core.AttachedProperties"
    xmlns:converters="clr-namespace:Arion.Style.Converters"
    xmlns:converters1="clr-namespace:StratumUi.Wpf.Core.Converters"
    xmlns:wpf="clr-namespace:Arion.Style.AttachedProperties">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="FontFamily.xaml" />
        <ResourceDictionary Source="Button.xaml" />
        <ResourceDictionary Source="Colors.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <converters1:BooleanToVisibilityConverter
        x:Key="BooleanToVisibilityConverter"
        FalseValue="Collapsed"
        TrueValue="Visible" />

    <Style x:Key="{x:Type DocumentViewer}" TargetType="{x:Type DocumentViewer}">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}" />
        <Setter Property="Background" Value="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="BorderBrush" Value="{StaticResource Dg2}" />
        <Setter Property="attachedProperties:DocumentViewer.ShowPrint" Value="False" />
        <Setter Property="attachedProperties:DocumentViewer.ShowContentCopy" Value="False" />
        <Setter Property="attachedProperties:DocumentViewer.ShowMagnifyPlus" Value="True" />
        <Setter Property="attachedProperties:DocumentViewer.ShowMagnifyMinus" Value="True" />
        <Setter Property="attachedProperties:DocumentViewer.ShowStretch" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DocumentViewer}">
                    <Border
                        Background="{StaticResource Pr100}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="6"
                        Focusable="False">
                        <Grid KeyboardNavigation.TabNavigation="Local">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <ToolBar
                                Background="Transparent"
                                KeyboardNavigation.TabNavigation="Continue"
                                ToolBarTray.IsLocked="True">
                                <Button
                                    attachedProperties:Button.Icon="Printer"
                                    Command="ApplicationCommands.Print"
                                    CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{StaticResource BtnText}"
                                    Visibility="{TemplateBinding attachedProperties:DocumentViewer.ShowPrint,
                                                                 Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <Button
                                    attachedProperties:Button.Icon="ContentCopy"
                                    Command="ApplicationCommands.Copy"
                                    CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{StaticResource BtnText}"
                                    Visibility="{TemplateBinding attachedProperties:DocumentViewer.ShowContentCopy,
                                                                 Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <Separator />
                                <Button
                                    attachedProperties:Button.Icon="MagnifyPlusOutline"
                                    Command="NavigationCommands.IncreaseZoom"
                                    CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{StaticResource BtnText}"
                                    Visibility="{TemplateBinding attachedProperties:DocumentViewer.ShowMagnifyPlus,
                                                                 Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <Button
                                    attachedProperties:Button.Icon="MagnifyMinusOutline"
                                    Command="NavigationCommands.DecreaseZoom"
                                    CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{StaticResource BtnText}"
                                    Visibility="{TemplateBinding attachedProperties:DocumentViewer.ShowMagnifyMinus,
                                                                 Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <Separator />
                                <Button
                                    attachedProperties:Button.Icon="StretchToPageOutline"
                                    Command="NavigationCommands.Zoom"
                                    CommandParameter="100.0"
                                    CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                    Style="{StaticResource BtnText}"
                                    Visibility="{TemplateBinding attachedProperties:DocumentViewer.ShowStretch,
                                                                 Converter={StaticResource BooleanToVisibilityConverter}}" />
                                <!--  <Button  -->
                                <!--  Command="DocumentViewer.FitToWidthCommand"  -->
                                <!--  CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"  -->
                                <!--  Content="Fit to Width"  -->
                                <!--  Style="{StaticResource BtnText}" />  -->
                                <!--  <Button  -->
                                <!--  Command="DocumentViewer.FitToMaxPagesAcrossCommand"  -->
                                <!--  CommandParameter="1"  -->
                                <!--  CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"  -->
                                <!--  Content="Whole Page"  -->
                                <!--  Style="{StaticResource BtnText}" />  -->
                                <!--  <Button  -->
                                <!--  Command="DocumentViewer.FitToMaxPagesAcrossCommand"  -->
                                <!--  CommandParameter="2"  -->
                                <!--  CommandTarget="{Binding RelativeSource={RelativeSource TemplatedParent}}"  -->
                                <!--  Content="Two Pages"  -->
                                <!--  Style="{StaticResource BtnText}" />  -->
                            </ToolBar>

                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Grid.Row="1"
                                Background="{StaticResource Lg5}"
                                CanContentScroll="true"
                                HorizontalScrollBarVisibility="Auto"
                                IsTabStop="true" />

                            <ContentControl x:Name="PART_FindToolBarHost" Grid.Row="2" />
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>