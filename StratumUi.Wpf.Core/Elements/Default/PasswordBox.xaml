<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:attached="clr-namespace:Arion.Style.AttachedProperties"
    xmlns:attachedProperties="clr-namespace:StratumUi.Wpf.Core.AttachedProperties"
    xmlns:commands1="clr-namespace:StratumUi.Wpf.Core.Commands"
    xmlns:controls1="clr-namespace:StratumUi.Wpf.Core.Controls"
    xmlns:converters="clr-namespace:Arion.Style.Converters"
    xmlns:converters1="clr-namespace:StratumUi.Wpf.Core.Converters">
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml" />
        <ResourceDictionary Source="Button.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <converters1:BooleanToVisibilityConverter
        x:Key="VisibilityConverter"
        FalseValue="Collapsed"
        TrueValue="Visible" />
    <converters1:BooleanToVisibilityConverter
        x:Key="PasswordBoxPlaceholderVisibility"
        FalseValue="Visible"
        TrueValue="Collapsed" />

    <Style TargetType="{x:Type PasswordBox}">
        <Setter Property="Height" Value="40" />
        <Setter Property="Foreground" Value="{StaticResource Dg2}" />
        <Setter Property="FontWeight" Value="Regular" />
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Cursor" Value="IBeam" />
        <Setter Property="BorderBrush" Value="{StaticResource Lg2}" />
        <Setter Property="Padding" Value="8,0" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="Background" Value="{StaticResource CleanW}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="attachedProperties:PasswordBox.MonitorPassword" Value="True" />
        <Setter Property="commands1:ClearText.HandlesClearCommand" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type PasswordBox}">
                    <Border
                        x:Name="Border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <controls1:Icon
                                Grid.Column="0"
                                Width="{TemplateBinding attachedProperties:TextBox.IconWidth}"
                                Height="{TemplateBinding attachedProperties:TextBox.IconHeight}"
                                Margin="8,0"
                                Fill="{TemplateBinding Foreground}"
                                Kind="{TemplateBinding attachedProperties:TextBox.LeftIcon}"
                                Visibility="{TemplateBinding attachedProperties:TextBox.LeftIcon,
                                                             Converter={StaticResource IconConverter}}" />

                            <ScrollViewer
                                x:Name="PART_ContentHost"
                                Grid.Column="1"
                                Width="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                Padding="{TemplateBinding Padding}"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="Center"
                                FontWeight="Medium"
                                IsTabStop="False"
                                TextElement.Foreground="{TemplateBinding Foreground}" />

                            <!--  PLACE HOLDER  -->
                            <TextBlock
                                x:Name="Placeholder"
                                Grid.Column="1"
                                Padding="10,0"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="Center"
                                FontSize="{TemplateBinding FontSize}"
                                FontWeight="{TemplateBinding FontWeight}"
                                Foreground="{StaticResource Mg4}"
                                IsHitTestVisible="False"
                                Text="{TemplateBinding attachedProperties:Hint.Placeholder}"
                                Visibility="{TemplateBinding attachedProperties:PasswordBox.HasContent,
                                                             Converter={StaticResource PasswordBoxPlaceholderVisibility}}" />

                            <!--  PASSWORD SECURED  -->
                            <TextBox
                                x:Name="securedPassword"
                                Grid.Column="1"
                                Width="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=ActualWidth}"
                                Margin="-1,0"
                                Padding="10,0"
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="Center"
                                attachedProperties:Hint.Placeholder="0"
                                BorderBrush="Transparent"
                                Cursor="IBeam"
                                FontWeight="{TemplateBinding FontWeight}"
                                HorizontalScrollBarVisibility="Hidden"
                                Text="{TemplateBinding attachedProperties:PasswordBox.SecuredPassword}"
                                TextElement.Foreground="{TemplateBinding Foreground}"
                                VerticalScrollBarVisibility="Hidden"
                                Visibility="Collapsed" />

                            <controls1:Icon
                                Grid.Column="2"
                                Width="{TemplateBinding attachedProperties:TextBox.IconWidth}"
                                Height="{TemplateBinding attachedProperties:TextBox.IconHeight}"
                                Margin="8,0"
                                Fill="{TemplateBinding Foreground}"
                                Kind="{TemplateBinding attachedProperties:TextBox.RightIcon}"
                                Visibility="{TemplateBinding attachedProperties:TextBox.RightIcon,
                                                             Converter={StaticResource IconConverter}}" />

                            <Button
                                Grid.Column="3"
                                Width="30"
                                Height="30"
                                Margin="5"
                                Padding="5"
                                attachedProperties:Button.Icon="Close"
                                Command="{x:Static commands1:ClearText.ClearCommand}"
                                Opacity=".4"
                                Style="{StaticResource BtnText}"
                                Visibility="{TemplateBinding attachedProperties:PasswordBox.CanClear,
                                                             Converter={StaticResource VisibilityConverter}}" />

                            <ToggleButton
                                x:Name="BtnShow"
                                Grid.Column="2"
                                Width="30"
                                Height="30"
                                Margin="5"
                                IsChecked="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(attachedProperties:PasswordBox.IsShowPassword)}"
                                Visibility="{TemplateBinding attachedProperties:PasswordBox.CanShowPassword,
                                                             Converter={StaticResource VisibilityConverter}}">
                                <ToggleButton.Style>
                                    <Style TargetType="{x:Type ToggleButton}">
                                        <Setter Property="Cursor" Value="Hand" />
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="{x:Type ToggleButton}">
                                                    <Border
                                                        Name="Border"
                                                        Width="{TemplateBinding Width}"
                                                        Height="{TemplateBinding Height}"
                                                        Background="Transparent"
                                                        BorderBrush="Transparent"
                                                        BorderThickness="0"
                                                        ClipToBounds="True"
                                                        CornerRadius="6"
                                                        Opacity=".4">
                                                        <Grid>
                                                            <controls1:Icon
                                                                x:Name="Icon"
                                                                Margin="0"
                                                                HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"
                                                                Fill="{TemplateBinding Foreground}"
                                                                Kind="Eye" />
                                                        </Grid>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsChecked" Value="True">
                                                            <Setter TargetName="Icon" Property="Fill" Value="{StaticResource Pr700}" />
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </ToggleButton.Style>
                            </ToggleButton>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="attachedProperties:PasswordBox.IsShowPassword" Value="True">
                            <Setter TargetName="securedPassword" Property="Visibility" Value="Visible" />
                            <Setter TargetName="PART_ContentHost" Property="Visibility" Value="Collapsed" />
                        </Trigger>
                        <Trigger Property="attachedProperties:PasswordBox.CanShowPassword" Value="True">
                            <Setter TargetName="PART_ContentHost" Property="Margin" Value="0,0,0,0" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FFFFFF" />
                            <Setter Property="BorderBrush" Value="#B0BABF" />
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Background" Value="#FFFFFF" />
                            <Setter Property="BorderBrush" Value="#5B6871" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Background" Value="#F6F8F9" />
                            <Setter Property="Foreground" Value="#EEF0F2" />
                            <Setter Property="BorderBrush" Value="#EEF0F2" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>